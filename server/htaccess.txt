# =============================================================================
# public_html/.htaccess  ·  1d-solutions.com
# =============================================================================
#
# Este archivo ES el .htaccess completo del servidor, listo para sustituir al
# actual. Todo lo que ya había se conserva palabra por palabra y en el mismo
# orden: no se ha modificado ni eliminado ninguna directiva existente.
#
# Lo añadido el 2026-08-15 por la auditoría de seguridad va entre marcas
# «INICIO AÑADIDO» y «FIN AÑADIDO», y va todo al final, después de lo original.
# Para deshacerlo basta con borrar esos bloques.
#
# Contexto y motivos: 1D-Web/server/README.md
#
# =============================================================================


# Redirige todos mis dominios a https://1d-solutions.com
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Protege el archivo htaccess
<files .htaccess>
order allow,deny
deny from all
</files>

<IfModule mod_expires.c>
# Enable expirations
ExpiresActive On
# Default directive
ExpiresDefault "access plus 1 days"
ExpiresByType application/x-msdownload "access 6 hour"
</IfModule>

<ifModule mod_headers.c>
    Header set Vary "Accept-Encoding"
</ifModule>

<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/css text/html text/javascript text/plain text/xml
    AddOutputFilterByType DEFLATE font/opentype font/otf font/ttf image/svg+xml image/x-icon
    AddOutputFilterByType DEFLATE application/vnd.ms-fontobject application/x-font-ttf
    AddOutputFilterByType DEFLATE application/x-font application/x-font-truetype
    AddOutputFilterByType DEFLATE application/x-font-opentype application/x-font-otf
    AddOutputFilterByType DEFLATE application/javascript application/x-javascript
    AddOutputFilterByType DEFLATE application/xhtml+xml application/xml application/rss+xml
</IfModule>


# ▼▼▼ INICIO AÑADIDO 2026-08-15 — 1. Cabeceras de seguridad ▼▼▼
#
# Hasta hoy la respuesta del sitio no traía ninguna cabecera de seguridad. El
# único "Header" que había arriba es Vary, que es de caché, no de seguridad.
#
# Se usa un <IfModule mod_headers.c> nuevo en vez de meter estas líneas en el
# que ya existe más arriba, para que el bloque añadido se pueda borrar entero
# sin tocar nada de lo original. Tener dos <IfModule> del mismo módulo es
# perfectamente válido en Apache.
#
# Ninguna de estas directivas cambia el contenido ni las rutas: sólo añaden
# cabeceras a la respuesta.
#
<IfModule mod_headers.c>

  # Obliga a HTTPS durante un año. El sitio ya lo fuerza con el RewriteRule de
  # arriba y el certificado de GoDaddy se renueva solo. Deliberadamente SIN
  # includeSubDomains (alcanzaría a un subdominio futuro sin certificado y lo
  # dejaría inaccesible) y SIN preload (es casi irreversible).
  #
  # Es la ÚNICA directiva de este archivo con efecto persistente en el visitante:
  # un navegador que la reciba usará HTTPS durante un año aunque mañana se quite.
  Header always set Strict-Transport-Security "max-age=31536000"

  # Impide que el navegador adivine el tipo de un archivo ignorando el
  # Content-Type. Sin esto, un .txt subido por error puede acabar ejecutándose
  # como HTML.
  Header always set X-Content-Type-Options "nosniff"

  # Nadie puede meter el sitio en un iframe salvo el propio sitio (clickjacking).
  # Ningún tercero embebe 1d-solutions.com: el checkout de PayPro es un enlace
  # que abre su dominio, no un iframe.
  Header always set X-Frame-Options "SAMEORIGIN"

  # Al salir hacia otro dominio se envía sólo el origen, no la URL completa.
  # Dentro del sitio se sigue enviando entera, que es lo que Analytics necesita
  # para el informe de páginas de referencia.
  Header always set Referrer-Policy "strict-origin-when-cross-origin"

  # El sitio no usa cámara, micrófono, geolocalización ni pagos del navegador.
  # Declararlo vacío impide que se los pida cualquier script inyectado — o
  # cualquier etiqueta que alguien añada a GTM sin pensarlo.
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=(), usb=()"

  # Oculta la versión de PHP si el hosting la anuncia.
  Header always unset X-Powered-By

</IfModule>
#
# ▲▲▲ FIN AÑADIDO — 1. Cabeceras de seguridad ▲▲▲


# ▼▼▼ INICIO AÑADIDO 2026-08-15 — 2. Content-Security-Policy (MODO INFORME) ▼▼▼
#
# "Report-Only" NO bloquea nada: sólo escribe las violaciones en la consola del
# navegador. Por definición no puede romper el sitio. Está así por dos motivos:
#
#   a) Todo el HTML lleva scripts en línea (el snippet de GTM, loadNonCritical,
#      addToCartSingle, el document.write del año). Una CSP sin 'unsafe-inline'
#      los mataría todos, y con 'unsafe-inline' la protección contra XSS es floja
#      igualmente. Lo que aporta de verdad es la lista de dominios permitidos.
#
#   b) GTM inyecta etiquetas en tiempo de ejecución. Añadir una etiqueta nueva en
#      GTM-TGXKJ8P puede introducir un dominio que no esté en esta lista, y en
#      modo bloqueo eso rompería la medición sin que nadie lo notase hasta pasadas
#      semanas — justo el modo de fallo que este proyecto persigue.
#
# LA LÍNEA DE ABAJO ES UNA SOLA, larguísima, A PROPÓSITO. Apache admite partirla
# con "\" al final, pero si al pegar queda un espacio detrás de la barra, Apache
# falla y el sitio devuelve 500. NO LA PARTAS al copiar.
#
# QUÉ REPORTA HOY. Medido el 2026-08-15 con un navegador real sobre producción
# (home, contact y product). Sólo dos cosas, y ningún dominio faltante:
#
#   15x  "Refused to evaluate a string as JavaScript" ('unsafe-eval')
#        Lo provoca GTM, que evalúa cadenas como código. EN MODO BLOQUEO GTM SE
#        ROMPERÍA, y con él toda la medición. Para pasar a bloqueo hay que elegir:
#        añadir 'unsafe-eval' al script-src —que debilita bastante la política,
#        porque es justo la capacidad que explota un XSS— o aceptar perder GTM.
#        No es una casilla que marcar: es la decisión de fondo, y por eso esta
#        cabecera sigue en modo informe.
#
#    3x  La hoja de estilo de cdn.jsdelivr.net (flag-icons), una por página.
#        Desaparece sola en cuanto se publique el build que la autoaloja. Si
#        sigue apareciendo después de publicar, es que el build no llegó a subirse.
#
# CÓMO PASARLA A BLOQUEO más adelante: repetir la medición sobre las 3 páginas ×
# 9 idiomas más el formulario y un clic de compra, resolver lo de 'unsafe-eval',
# y sólo entonces quitar el sufijo "-Report-Only" del nombre de la cabecera.
#
<IfModule mod_headers.c>
  Header always set Content-Security-Policy-Report-Only "default-src 'self'; script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://www.google-analytics.com https://googleads.g.doubleclick.net https://www.googleadservices.com https://www.google.com https://web3forms.com https://*.hcaptcha.com https://hcaptcha.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://*.hcaptcha.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: https://www.googletagmanager.com https://www.google-analytics.com https://www.google.com https://googleads.g.doubleclick.net https://td.doubleclick.net https://pagead2.googlesyndication.com; connect-src 'self' https://www.google-analytics.com https://region1.google-analytics.com https://analytics.google.com https://www.googletagmanager.com https://api.web3forms.com https://pagead2.googlesyndication.com https://*.hcaptcha.com; frame-src https://www.googletagmanager.com https://td.doubleclick.net https://*.hcaptcha.com https://hcaptcha.com; form-action 'self' https://api.web3forms.com https://store.payproglobal.com; base-uri 'self'; object-src 'none'; frame-ancestors 'self'"
</IfModule>
#
# ▲▲▲ FIN AÑADIDO — 2. Content-Security-Policy ▲▲▲


# ▼▼▼ INICIO AÑADIDO 2026-08-15 — 3. Redirección de las páginas de /1dnest/ ▼▼▼
#
# Cuatro páginas de 2021-2024 que seguían respondiendo 200, con analítica vieja
# (UA-209685-2, una propiedad Universal Analytics que ya ni recoge datos) y
# enlaces a ShareIt, la pasarela anterior a PayPro. Además cargaban
# https://cdn.ywxi.net/js/1.js — el sello de TrustedSite, 42 KB de JavaScript de
# terceros con ejecución completa, en páginas que no mantiene nadie.
#
# 301 en vez de borrarlas: conserva el poco enlazado externo que tengan y evita
# 404 en Search Console. Van al español porque el contenido estaba en español.
#
# ESTAS SON LAS ÚNICAS URLs DEL SITIO CUYO COMPORTAMIENTO CAMBIA. Ninguna página
# viva pasa por aquí.
#
<IfModule mod_alias.c>
  Redirect 301 /1dnest/product.html  https://1d-solutions.com/es/product.html
  Redirect 301 /1dnest/indice.html   https://1d-solutions.com/es/index.html
  Redirect 301 /1dnest/1DNest.html   https://1d-solutions.com/es/index.html

  # AFINADO 2026-08-27: estas dos apuntaban a /es/index.html como todo lo demás.
  # Al leer el <title> original se vio que library.html era «1D Solutions, DLL»
  # (la CutLib) y contac.html era «Contacto». Mandar a quien buscaba la DLL a la
  # página donde figura con su precio convierte mejor que dejarlo en la portada,
  # y Google premia que el destino de una 301 sea relevante.
  Redirect 301 /1dnest/library.html  https://1d-solutions.com/es/product.html
  Redirect 301 /1dnest/contac.html   https://1d-solutions.com/es/contact.html

  # Cualquier otra cosa colgando de /1dnest/ va a la home en español. Va al final
  # porque gana la primera regla que encaja: las cuatro de arriba deben poder ganar.
  #
  # RedirectMatch y no Redirect, y no es estilo: "Redirect" casa por prefijo y
  # AÑADE al destino lo que sobra de la ruta, así que /1dnest/otra.html habría
  # acabado en ".../es/index.htmlotra.html". Se descubrió probándolo contra un
  # Apache local el 2026-08-15. RedirectMatch sin grupos usa el destino tal cual.
  RedirectMatch 301 ^/1dnest/ https://1d-solutions.com/es/index.html
</IfModule>
#
# ▲▲▲ FIN AÑADIDO — 3. Redirección de /1dnest/ ▲▲▲


# ▼▼▼ INICIO AÑADIDO 2026-08-15 — 4. Archivos que no deben servirse nunca ▼▼▼
#
# Hoy ninguno de estos existe en public_html (comprobado el 2026-08-15: todos
# daban 404). Es una red de seguridad para el día en que alguien suba una carpeta
# de más: el despliegue es arrastrar archivos a mano, y ya pasó una vez que el
# sitio entero acabara duplicado en public_html/theme/.
#
# Sobre "^\." (archivos que empiezan por punto): SÍ hace falta. Parecía que este
# hosting ya los bloqueaba, porque /.env y /.git/config devolvían 403 — pero
# /.DS_Store devolvía 404, y si el bloqueo fuese por patrón habría dado 403
# existiera o no. O sea: GoDaddy bloquea .env y .git por nombre, no el patrón.
# Y .DS_Store es justo el que puede colarse en una subida a mano, filtrando el
# listado del directorio.
#
# Que este bloque alcance también al .htaccess, que ya tiene su <files> con
# sintaxis de Apache 2.2 más arriba, se probó explícitamente: sigue devolviendo
# 403 con los dos bloques activos. Son contextos distintos, así que no es la
# mezcla de modelos de autorización que desaconseja la documentación de Apache.
#
# El patrón no toca .html, .css, .js, .svg, .webp ni nada que el sitio sirva. Los
# .exe y .zip de /_downloads/ siguen funcionando. El .zip estuvo en la lista hasta
# el 2026-08-18: circulan enlaces externos a /_downloads/*.zip (p. ej.
# 1dn402_25.zip) que devolvían 403, así que se quitó — editado a mano en cPanel y
# sincronizado aquí después.
#
<FilesMatch "(^\.|composer\.(json|lock)|package(-lock)?\.json|gulpfile\.js|netlify\.toml|\.(sh|bak|sql|log|md)$)">
  # El guardia IfModule no es decorativo: "Require" es sintaxis de Apache 2.4 y
  # en 2.2 provoca un 500 en TODO el sitio, no sólo en los archivos afectados.
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>

# El listado de directorios ya venía desactivado en este hosting; se deja
# explícito para que no dependa de la configuración global de GoDaddy.
<IfModule mod_autoindex.c>
  Options -Indexes
</IfModule>
#
# ▲▲▲ FIN AÑADIDO — 4. Archivos que no deben servirse nunca ▲▲▲


# ▼▼▼ INICIO AÑADIDO 2026-08-26 — 5. SEO: www → sin-www, y los zombis de /pt/ ▼▼▼
#
# a) www.1d-solutions.com servía el sitio COMPLETO con 200, sin redirigir
#    (verificado el 2026-08-26): un duplicado entero a ojos de Google. La regla
#    de HTTPS de arriba conserva el host (%{HTTP_HOST}), así que nunca quitó el
#    www pese a lo que dice su comentario. Esta regla lo hace. El certificado
#    cubre www (curl -sI https://www... respondía 200 sin error TLS), así que
#    la redirección funciona también para quien entre por https://www.
#
#    Una petición http://www hará dos saltos (→ https://www → https://apex):
#    aceptado a propósito para no tocar la regla original de arriba.
#
RewriteCond %{HTTP_HOST} ^www\.1d-solutions\.com$ [NC]
RewriteRule ^(.*)$ https://1d-solutions.com/$1 [L,R=301]

# b) Los dos restos de /pt/ del sitio viejo (ver la nota sitio-zombi-1dnest del
#    vault de 1d-ads). Los archivos se BORRARON en la limpieza de public_html
#    del 2026-08-27 y hoy devuelven 404; estas 301 quedan para el enlazado
#    externo antiguo que pueda apuntar ahí: mejor 301 al equivalente vivo que
#    un 404. El bloque 3 solo cubría /1dnest/.
#
<IfModule mod_alias.c>
  Redirect 301 /pt/indice.html   https://1d-solutions.com/pt/index.html
  Redirect 301 /pt/library.html  https://1d-solutions.com/pt/index.html
</IfModule>

# c) La carpeta /esp/ del sitio viejo (anterior a /es/). Search Console registra
#    147 clics y 1.261 impresiones, y GA4 unas 480 sesiones sólo para
#    /esp/index.html. NO la borró la limpieza del 2026-08-27: la carpeta no está
#    ni en public_html ni en _privado, y la última visita en GA4 es del
#    2025-07-08. Lleva devolviendo 404 desde julio de 2025, trece meses.
#
<IfModule mod_alias.c>
  Redirect 301 /esp/index.html    https://1d-solutions.com/es/index.html
  Redirect 301 /esp/product.html  https://1d-solutions.com/es/product.html
  Redirect 301 /esp/contact.html  https://1d-solutions.com/es/contact.html
  RedirectMatch 301 ^/esp/?$      https://1d-solutions.com/es/index.html
</IfModule>
#
# ▲▲▲ FIN AÑADIDO — 5. SEO ▲▲▲


# ▼▼▼ INICIO AÑADIDO 2026-08-27 — 6. Rutas antiguas que GA4 delató ▼▼▼
#
# POR QUÉ ESTE BLOQUE EXISTE, Y POR QUÉ NO BASTABA CON SEARCH CONSOLE.
#
# La auditoría del 2026-08-27 (nota auditoria-urls-legacy-y-pasarela del vault de
# 1d-ads) empezó midiendo con Search Console, que sólo ve la búsqueda de Google.
# Al cruzar con GA4 aparecieron rutas con tráfico REAL y reciente que Search
# Console no reporta por definición, porque llega por Bing, DuckDuckGo, Ecosia,
# ChatGPT, enlace directo y marcadores:
#
#   /library.html          287 sesiones (135 orgánico Google, 75 de PAGO) —
#                          el legacy con MÁS tráfico medido, y CERO filas en
#                          Search Console. Era la página de la DLL CutLib.
#   /z-OLD/index.html       52 sesiones (36 directo, 8 de pago, 5 Google, Bing…)
#   /z-OLD/                 45 sesiones (39 de Bing, y nada de Google)
#   /z-OLD/product.html     26 sesiones
#   /z-OLD/library.html      8 sesiones — título «DLL of 1D Cutting Optimization»
#
# Con Consent Mode denegando por defecto, GA4 ve del orden de 1 de cada 5
# sesiones, así que TODAS esas cifras son un suelo, no un total.
#
# Y todas esas páginas llevaban enlaces a order.shareit.com, una pasarela MUERTA
# que no cobra: quien llegaba no podía comprar. Ahora devuelven 404, que pierde
# al visitante igual. Con estas 301 acaba en una página viva donde sí se vende.
#
# Destinos elegidos por el idioma y el tema del <title> original, no al bulto:
# lo de la DLL va a la página de producto (donde CutLib figura con su precio),
# y /z-OLD/ estaba en inglés, así que va a la raíz inglesa y no a /es/.
#
<IfModule mod_alias.c>
  # La DLL / CutLib, en la raíz y en sus copias antiguas
  Redirect 301 /library.html          https://1d-solutions.com/product.html
  Redirect 301 /z-OLD/library.html    https://1d-solutions.com/product.html
  Redirect 301 /z-OLD/product.html    https://1d-solutions.com/product.html
  Redirect 301 /z-OLD/contac.html     https://1d-solutions.com/es/contact.html
  Redirect 301 /z-OLD/index.html      https://1d-solutions.com/index.html

  # Restos en la raíz del sitio de los 2000 (todos 404 hoy)
  Redirect 301 /contac.html           https://1d-solutions.com/contact.html
  Redirect 301 /indice.html           https://1d-solutions.com/es/index.html

  # ¡CUIDADO! Estas dos van con RedirectMatch y con «$» final A PROPÓSITO.
  # «Redirect 301 /index.htm» casaría también con /index.html —la home— porque
  # casa por PREFIJO y añade el resto al destino: /index.html acabaría
  # redirigido a /index.htm + "l" = /index.htmll, tumbando la portada. Lo mismo
  # con /product.htm y /product.html. El «^...$» impide el prefijo.
  RedirectMatch 301 ^/index\.htm$     https://1d-solutions.com/index.html
  RedirectMatch 301 ^/product\.htm$   https://1d-solutions.com/product.html

  # Borradores y carpetas de pruebas que aún reciben algún enlace externo.
  # Iban en inglés («1D Solutions - Length Cutting Optimizer»).
  Redirect 301 /index_EX.html         https://1d-solutions.com/index.html
  Redirect 301 /en/index1.html        https://1d-solutions.com/index.html
  Redirect 301 /en/product1.html      https://1d-solutions.com/product.html

  # La página de venta que sobrevivió dentro del canal de descargas: la carpeta
  # entp/ se conservó por sus instaladores por cliente, y nadie sabía que dentro
  # había un product.html completo sirviendo 200 con enlaces a ShareIt. Es el
  # único .htm/.html de todo _downloads/.
  # Esta 301 surte efecto AUNQUE EL ARCHIVO SIGA AHÍ: mod_alias resuelve en la
  # fase de traducción de la URL, antes de mapear al sistema de archivos. O sea
  # que basta con subir este .htaccess para cerrar el callejón sin salida, sin
  # esperar a borrar el fichero. Borrarlo después sigue siendo lo correcto.
  Redirect 301 /_downloads/entp/product.html  https://1d-solutions.com/product.html

  # Los restos de /pt/ que el bloque 5b no cubría. El .htm va anclado por lo
  # mismo que arriba: si no, casaría con /pt/indice.html.
  RedirectMatch 301 ^/pt/indice\.htm$ https://1d-solutions.com/pt/index.html
  Redirect 301 /pt/contac.html        https://1d-solutions.com/pt/contact.html
  Redirect 301 /pt/page1.html         https://1d-solutions.com/pt/index.html

  # Catch-all de las carpetas muertas, AL FINAL: gana la primera regla que
  # encaja, así que las específicas de arriba deben poder ganar. RedirectMatch y
  # no Redirect, porque «Redirect» casa por prefijo y AÑADE al destino lo que
  # sobra de la ruta (la trampa documentada en el bloque 3).
  RedirectMatch 301 ^/z-OLD/    https://1d-solutions.com/index.html
  RedirectMatch 301 ^/nuevo/    https://1d-solutions.com/es/index.html
  RedirectMatch 301 ^/new/      https://1d-solutions.com/index.html
  RedirectMatch 301 ^/prueba/   https://1d-solutions.com/index.html
  RedirectMatch 301 ^/J1DN/     https://1d-solutions.com/index.html
</IfModule>
#
# NOTA sobre /1dnest/: sus 301 están en el bloque 3 y funcionan, pero mandan
# TODO a /es/index.html salvo product.html. Con los títulos originales a la vista
# hay destinos mejores (/1dnest/library.html era la DLL, /1dnest/contac.html era
# contacto). No se cambia aquí para no tocar un bloque que ya está en producción
# y verificado; queda como mejora de calidad en el vault.
#
# ▲▲▲ FIN AÑADIDO — 6. Rutas antiguas que GA4 delató ▲▲▲
