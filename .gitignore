Thumbs.db
.DS_Store
.dist
.tmp
.sass-cache
npm-debug.log
node_modules
package-lock.json
yarn.lock
netlify
theme/

# Copias sueltas del build. Finder crea "theme 2/" al descomprimir junto a una
# theme/ existente, y los zips son el artefacto que se sube al hosting. Ninguno
# debe entrar en git: son la salida del build, no la fuente. Y una copia vieja
# a mano tirada en la raíz es justo lo que hace falta para publicar por error
# una versión de hace seis meses.
theme */
*.zip

# Volcados de public_html bajados del servidor para auditar. Contienen datos de
# clientes (Docus/, _downloads/) y nunca deben entrar en git.
1d_public_html_*/

# Credenciales SSH del hosting para scripts/publicar.sh. La plantilla
# commiteada es publicar.env.ejemplo; el archivo real no entra nunca en git.
scripts/publicar.env